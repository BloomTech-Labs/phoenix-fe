import { makeStyles } from '@material-ui/core/styles';

const EventPageStyle = makeStyles(theme => ({
    root: {
        flexGrow: 10,
        margin: "auto",
    },
    card: {
        padding: "1rem"
    }


}))

export default EventPageStyle;